import { lazyloadMedias } from './medias';

export default {
  medias: lazyloadMedias,
  src: 'src',
  srcset: 'srcset',
  poster: 'poster',
  bg: 'bg',
  classes: {
    loading: 'loading',
    loaded: 'loaded',
    error: 'error',
    applied: 'applied',
  },
};
